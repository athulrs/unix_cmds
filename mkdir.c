#include<stdio.h>
#include<conio.h>
#include<process.h>
#include<dir.h>
void main() 
{
   int n;
   char *dir;
   gets(dir);
   n = mkdir(dirname);
   if(!check)
    printf("Directory created\n");
   else
    exit(1);
    getch();
    system("dir/p");
    getch();
}

